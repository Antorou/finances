<p><%= @transaction.name %></p>

<p><%= @transaction.description %></p>

<p><%= @transaction.amount %></p>

<p><%= @transaction.date %></p>

<%= link_to 'Edit', edit_transaction_path(@transaction), class: 'btn btn-primary' %>
<%= link_to 'Delete', transaction_path(@transaction), method: :delete, data: { confirm: 'Are you sure you want to delete this transaction?' }, class: 'btn btn-danger' %>
